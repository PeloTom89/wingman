*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
build/
/captures/
.externalNativeBuild/
.cxx/
*.apk
*.ap_
*.dex
*.class

# DJI App Key must never be committed in plaintext into version control history;
# it is injected via local.properties (gitignored) into the manifest placeholder.
